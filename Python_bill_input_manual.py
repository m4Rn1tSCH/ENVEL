#find columns first
bills_found = []

try:
    column_list = files_in.items()
    if column_list.str(get(bill_list)):
        for n in bill_list:
            bills_found.append(bill_list)
    else:
        print("Sorry, no known bills recognized :*(")
except:
#specify input manually
#if no bill was found and list remains empty
    if bills_found == []:
        pass 
        print("specify manually")