#Packages
import datetime as dt
import schedule as sdl
import request


stock_data = []
soup = []
def print_time() :
	print(dt.DateTime)
	
def GET_prices():
        sdl.every(5).seconds.do(print_time)
        run 

while True:
    sdl.run_pending()
    time.sleep(3)
for minute in soup:
	stock_data = stock_data.append(soup)

dt.DateTime.DayOfWeek = Sunday
dt.DateTime.DayOfWeek = Saturday


